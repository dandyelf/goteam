syntax = "proto3";
option go_package = "./prot";
import "google/protobuf/timestamp.proto";

// Определение сервиса.
service RepServ {
  // определение метода с использованием rpc.
  rpc GetData(Point) returns (Point) {}

}

message Point {
  string session_id = 1;
  double frequency = 2;
  google.protobuf.Timestamp timestamp = 3;
}
